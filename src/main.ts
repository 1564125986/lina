/**
 * Main TypeScript entry point
 * This file serves as an entry point for the Vite build system
 * It doesn't do much since we're using vanilla JS in the main files
 */

// Import CSS
import '../static/css/theme.css';
import '../static/css/main.css';

// Import JavaScript
import '../static/js/main.js';

// Export empty module
export {};
